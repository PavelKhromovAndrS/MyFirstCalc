<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyFirstCalc" parent="Theme.MaterialComponents.Light.DarkActionBar">
       <item name="materialButtonStyle">@style/CalcButtonTheme1</item>
        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>
    <style name="Theme.MyFirstCalc1" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="materialButtonStyle">@style/CalcButtonTheme2</item>
        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>
    <style name="Theme.MyFirstCalc2" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="materialButtonStyle">@style/CalcButtonTheme3</item>
        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>
    <style name="Theme.MyFirstCalc3" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="materialButtonStyle">@style/CalcButtonTheme4</item>
        <!-- Customize your theme here. -->
        <item name="android:textSize">@dimen/button_text_size</item>
    </style>
</resources>